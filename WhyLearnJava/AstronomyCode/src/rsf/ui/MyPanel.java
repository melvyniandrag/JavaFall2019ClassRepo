package rsf.ui;

// Add a couple of variables to JPanel that specify the size of the JPanel.

import javax.swing.JPanel;

public class MyPanel extends JPanel {

  // Initial dimensions of the drawing pane.
  public int initialWidth = 500;
  public int initialHeight = 500;
}